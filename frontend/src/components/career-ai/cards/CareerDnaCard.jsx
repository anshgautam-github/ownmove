/**
 * Deprecated — "Career DNA" was fully replaced by "Career Signals"
 * (see CareerSignalsCard.jsx) as part of the "Career Intelligence Report"
 * redesign. That version led with a synthesized personality "archetype"
 * (e.g. "The Builder"); the report no longer generates archetypes or
 * personality labels at all — see ProfileDiagnosis/CareerSignal's
 * docstrings in backend/app/profile_analysis/schemas/analysis.py for why.
 *
 * Kept in place (empty of logic) rather than deleted since files already
 * written to this workspace can't be removed outright. Nothing imports
 * this file — see ProfileAnalysisDashboard.jsx, which renders
 * CareerSignalsCard instead.
 */
export {};

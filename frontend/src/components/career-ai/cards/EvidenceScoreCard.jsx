/**
 * Deprecated — "Evidence Score" was replaced by "Evidence Credibility"
 * (see EvidenceCredibilityCard.jsx) as part of the "Career Intelligence
 * Report" redesign: instead of a bare skill confidence bar, that section
 * shows a named credibility verdict (verified / partially_verified /
 * self_reported) plus explicit supporting and missing evidence per claimed
 * capability.
 *
 * Kept in place (empty of logic) rather than deleted since files already
 * written to this workspace can't be removed outright. Nothing imports
 * this file — see ProfileAnalysisDashboard.jsx, which renders
 * EvidenceCredibilityCard instead.
 */
export {};

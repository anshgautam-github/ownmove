/**
 * Deprecated — "Recruiter View" (strengths/concerns side by side, plus a
 * final_summary paragraph) was retired as part of the "Career Intelligence
 * Report" redesign: it overlapped heavily with two other sections once they
 * existed — Profile Diagnosis (which now owns the strongest-signal/
 * limiting-factor verdict and the one narrative paragraph) and Score
 * Breakdown (which now owns the itemized positive/negative factor list).
 * Keeping both would have violated the redesign's own "every card must
 * answer a unique question" principle.
 *
 * Kept in place (empty of logic) rather than deleted since files already
 * written to this workspace can't be removed outright. Nothing imports
 * this file — see ProfileAnalysisDashboard.jsx, which renders
 * ProfileDiagnosisCard and ScoreBreakdownCard instead.
 */
export {};
